// Re-export page handler from the framework
// All logic (switch, dynamic imports, data loading) is handled in the package
export { default, generateMetadata } from '@shopana/storefront-sdk/next/page';
